<%= simple_form_for @dressage_test do |f| %>
  <%= f.input :org_name %>
  <%= f.input :name %>
  <%= f.input :level %>
  <%= f.input :year %>

Look how to input assoicated inputs in. 

<%= f.button :submit %>

<% end %>
