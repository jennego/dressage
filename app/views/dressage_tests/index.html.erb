

<div class="container">

<%#
  Filterrific adds the `form_for_filterrific` view helper:
  * adds dom id 'filterrific_filter'
  * applies javascript behaviors:
      * AJAX form submission on change
      * AJAX spinner while AJAX request is being processed
  * sets form_for options like :url, :method and input name prefix
%>
<%= form_for_filterrific @filterrific,  html: { id: 'filterrific-no-ajax-auto-submit' , class:'filter-form'}  do |f|%>
  <div class="form-group">
    Search
    <%# give the search field the 'filterrific-periodically-observed' class for live updates %>
    <%= f.text_field(
      :search_query,
      class: 'form-control'
    ) %>
  </div>
  <div class="form-group">
    Sorted by
    <%= f.select(:sorted_by, @filterrific.select_options[:sorted_by],{}, {:class => 'form-control'}) %>
  </div>
  <div class="form-group">
  Level
      <%= f.select(
      :filter_by_level,
      @filterrific.select_options[:filter_by_level], 
      { include_blank: '- Any -' },
      {class: 'form-control'} 
      ) %>
  </div>
  <div class="form-group">
    <%= link_to(
      'Reset filters',
      reset_filterrific_url,
      class: "btn btn-danger" 
    ) %>
  <%= f.submit 'Filter', class: "btn btn-primary" %>
  </div>
  <%# add an automated spinner to your form when the list is refreshed %>
  <%= render_filterrific_spinner %>
<% end %>

<%= render(
  partial: 'dressage_tests/list',
  locals: { dressage_tests: @dressage_tests }
) %>



<div class="row">

  <div class="col">

     <div class="list-group">
           <% @dressage_tests.each_with_index do |d, i| %>
      <% if d.level == "Walk/Trot"  %>
      <% if i == 4 %>
      <%= puts i %>
      <%= puts d %>
      <li class="list-group-item list-group-item bg-dark text-white"> Walk/Trot</li>
      <% end %>
        <%= link_to (dressage_test_path(d)), class: "list-group-item list-group-item-action" do %>
        <%= d.year %> <%= d.org_name %> <%= d.level %> <%=d.name %> 
        <% end %>
     <% end %> 
     <% end %>

    </div>
  </div>

  <div class="col">

    <div class="list-group">
           <% @dressage_tests.each_with_index do |d, i| %>
      <% if d.level == "Training"  %>
      <% if i == 0 %>
      <li class="list-group-item list-group-item bg-dark text-white"> Training</li>
      <% end %>
        <%= link_to (dressage_test_path(d)), class: "list-group-item list-group-item-action" do %>
        <%= d.year %> <%= d.org_name %> <%= d.level %> <%=d.name %> 
        <% end %>
     <% end %> 
     <% end %>

    </div>

  </div>

  <div class="col">

    <div class="list-group">
      <li class="list-group-item list-group-item bg-dark text-white"> First Level</li>
      <a href="#" class="list-group-item list-group-item-action">Morbi leo risus</a>
      <a href="#" class="list-group-item list-group-item-action">Porta ac consectetur ac</a>
      <a href="#" class="list-group-item list-group-item-action">Vestibulum at eros</a>
    </div>

  </div>

  </div>
 
</div>

</div>

